---
interface Props { href: string; title: string; description: string; }
const { href, title, description } = Astro.props;
---
<article class="card"><h2><a href={href}>{title}<span aria-hidden="true"> ↗</span></a></h2><p>{description}</p></article>
